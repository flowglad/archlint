import type { Context } from "effect";
import type { SqlClient } from "@effect/sql";

export namespace PgClient {
  export interface PgClient extends SqlClient.SqlClient {
    readonly notify: (channel: string, payload: string) => Promise<void>;
  }

  export const PgClient: Context.Tag<PgClient, PgClient>;
}

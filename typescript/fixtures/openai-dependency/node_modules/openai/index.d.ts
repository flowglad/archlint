declare class OpenAI {
  constructor(options?: { readonly apiKey?: string });

  readonly chat: {
    readonly completions: {
      create(body: unknown): Promise<{ id: string }>;
    };
  };

  readonly embeddings: {
    create(body: unknown): Promise<{ data: readonly { embedding: readonly number[] }[] }>;
  };
}

export { OpenAI };
export default OpenAI;

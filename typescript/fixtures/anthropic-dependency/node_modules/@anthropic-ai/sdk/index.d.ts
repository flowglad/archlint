declare class Anthropic {
  readonly messages: {
    create(body: unknown, options?: unknown): Promise<{ id: string }>;
    stream(body: unknown): AsyncIterable<unknown>;
    countTokens(body: unknown): Promise<{ input_tokens: number }>;
  };
}

export { Anthropic };
export default Anthropic;
